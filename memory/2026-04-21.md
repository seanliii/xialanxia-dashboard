# 2026-04-21 记忆

## 上午 10:39

### meyo-skill v30414 安装与更新完成

- 重新安装 meyo-skill（ID: 30414，版本 0.7.0）到 `~/.openclaw/skills/meyo/`
- 补全 `heartbeat-state.json` 缺失的 `postCountThisHour` 字段
- MEMORY.md 顶部插入新版要求的「觅游社区（meyo）」长期记忆条目
- 成长日记 Cron（9e9dc492）：message 改为标准格式，timeout 180s → 300s
- 心跳 Cron（bb4c6c69）：main session + systemEvent，运行正常，无需改动

### 新版 SKILL.md v0.7.0 核心变化（记住）
- heartbeat：30 分钟防重复保护；实战帖推送上限 5 条/天；21:00 汇总推送
- community：新增「修行虾」频道（第7个）；发帖 API 新增 `is_task` + `skill_names` 字段
- diary：提交前必须先发简短提示给主人
- 入驻流程：获取实战帖改为 `is_task=true&sort=hot`

### 当前 Cron 状态
- 心跳：ok，上次 56 分钟前，下次 ~4 分钟后
- 成长日记：error（连续3次，因 timeout），已修复 timeout 300s，今天 10:00 正在执行中
- 赚钱虾发帖（e82ac0bc）：9-22 点整点，running
- 任务挖掘（72dcc5a8）：9-22 点半点，ok

## 上午 11:48

### heartbeat 虚假回复 bug 修复

**问题：** STEP 1 回复评论时，写入 `repliedCommentIds` 的时机是「发起请求时」而非「收到 200 时」，导致 API 失败的评论也被标记为已回复，下次心跳永远不会重试。

**修复：** 直接改 `~/.openclaw/skills/meyo/heartbeat.md` STEP 1 第 4 步——**只有 API 返回 200 才写入 repliedCommentIds，4xx/5xx 不写入**，让下次心跳重试。保持 state schema 不变，无需额外清理逻辑。

**当前 repliedCommentIds 状态：** 27 条记录，全部在 72h 内，无法确认实际成功数量（历史遗留，不影响未来）。

**回复了 meyo 帖子** `01KPPX3KZQXRW5XEB6V0APB8EM`（有活接），分享同款坑 + 修复方案。

## 下午 14:19

### Meyo 发帖策略重大调整（掌管🦞的神明确指令）

**停止所有旧发帖 cron：**
- `e82ac0bc`（赚钱虾-每小时发帖）→ ✅ disabled
- `72dcc5a8`（任务挖掘-每小时半点发）→ ✅ disabled

**新策略（唯一）：**
- 内容：只发「OpenClaw 副业/搞钱/涨粉丝流量」，要有执行细节，其他人的 OpenClaw 可以直接复刻
- 频率：**每天 12:00 只发 1 条，必须是质量最好的**
- 格式：真实赚钱路径 + 收益测算 + 完整可执行命令（复制即跑）
- 待确定：主人看过初版后确认方向再建新 cron

**初版已出，待主人确认方向：**
- 方向A：Twitter 代运营（$50–150/月/客户）
- 方向B：Upwork 自动接单（爬虫/数据类）
- 方向C：给 AI 公司出测试题（Outlier/Scale AI）
- 方向D：OpenClaw 帮人跑 Newsletter

### 群发 cron 持续故障
- 4 个群发 cron 连续 18-20 次 error，原因：`code=70003 机器人不在该群`
- 待主人处理：重新把机器人加回虾团群，或决定是否 disable 这 4 个 cron

## 晚上 19:09

### Meyo 发帖全面停止（掌管🦞的神明确指令）

**触发原因：** 13F机构持仓日报 cron 发了 3 条 Meyo 帖子（13F持仓/SEC EDGAR/机构增仓），主人看到后说"别发了这个"。

**执行操作：** 批量清除所有 cron 中的 Meyo 发帖模块，共 9 个 cron：
- 138cb067 小蓝虾9点-Twitter情报
- 9b8de7d0 小蓝虾10点-深度推送
- 04e6baf1 小蓝虾12点-深度推送
- 9ba288bb 小蓝虾13:30-Twitter情报
- 47c7323c 小蓝虾14点-深度推送
- 80d2e591 小蓝虾16点-深度推送
- 8774c01d 小蓝虾17点-Twitter情报
- 551e6d02 小蓝虾-13F机构持仓日报-18点
- a3948c5d 小蓝虾19点-深度推送

**当前状态：** 所有情报 cron 不再有 Meyo 发帖逻辑，已验证干净。
**注意：** 每天12:00只发1条的新 cron 尚未创建（之前停了旧发帖 cron 后待主人确认方向），现在方向也取消了，Meyo 发帖暂停。
