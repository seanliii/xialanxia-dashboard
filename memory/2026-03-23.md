# 2026-03-23 日志

## 10:08 — Dashboard 三问题修复

### 问题汇总（用户报告）
1. Dashboard 日志、记忆没更新
2. 持仓界面没了（蓝虾账户 stat div 重复）
3. 价格停留在 03-19

### 根因分析

**问题1 — 蓝虾 stat div 重复追加**
- `sync_dashboard.py` 第459行 regex：`r'...#f87171">.*?</div>'`
- `.*?` 非贪婪只匹配到第一个 `</div>`，没有完整覆盖嵌套的 div block
- 每次 cron 运行 sync_dashboard.py 就在末尾追加一个新的 stat-val+stat-sub
- 累积20次导致 Dashboard 持仓界面无法正常显示

**问题2 — 价格更新停留在 03-19**
- portfolio_scanner.py 的 Stooq fetch 在沙盒网络下超时（需要代理）
- 03-22 之前 cron 跑 scanner 但价格拿不到（静默失败，保留旧值）

### 修复操作
1. **Python 脚本清理蓝虾 div 重复**：用 `re.sub` 找到包含20个重复块的节点，只保留最后一个（最新值）
2. **sync_dashboard.py regex 修复**：改用精确匹配 `stat-lbl` 内容锁定边界，彻底防止重复追加
3. **portfolio_scanner.py 手动触发**：今日 10:09 成功跑了一次，获取到 Stooq 价格（1只成功，其余从 structure.json 取旧值）
4. **手动更新 structure.json 汇总数据**：三账户当前总资产 $148,386（-1.08%）
5. **Dashboard 上传 S3 403**：S3plus token 过期/权限问题，已 commit 到 git

### 当前持仓快照（10:09 Stooq）
| 账户 | 总资产 | 盈亏 |
|------|--------|------|
| 🏦 大机构 | $50,490 | +$490 (+1.0%) |
| ⚡ 激进 | $48,251 | -$1,749 (-3.5%) |
| 🦐 蓝虾 | $49,645 | -$355 (-0.7%) |
| **合计** | **$148,386** | **-$1,614 (-1.08%)** |

大机构：CPNG(-3%) HBAN(-2%) CVNA(-3.3%) VST(+14.8%) TSM(+0%)
激进：UPXI(-7.8%) QUBT(-7.7%) SOUN(-15.6%) IREN(-3.9%)
蓝虾：ROLR(-12.2%) TOI(+0.3%)

### 待修复
- S3plus 上传 403 需要重新获取 token 或配置
- 价格获取网络问题（Stooq/Yahoo 都需要代理，但 proxy 配置有时超时）
- 03-19 至 03-22 记忆空档（cron memory_compressor 未正常运行）

## 20:06 — 晚间 heartbeat 检查

- 外网持续不通，Yahoo Finance / Stooq / Twitter 均无法访问
- S3plus 403 问题未解决，Dashboard 最后同步停留在 03-19
- 今日有效 cron 推送：1000-scan、1630-check（均在离线模式下完成）
- 持仓价格冻结在上次成功抓取值（03-19 或更早）
- 无止损触发，所有标的安全距离 >8%
- **明日优先事项**：
  1. 确认网络恢复 → 立刻跑 portfolio_scanner.py 更新价格
  2. 修复 S3plus token → 上传 Dashboard
  3. 补跑 memory_compressor（03-19~03-23 记忆空档）
