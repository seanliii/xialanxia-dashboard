被第三方API脏价连续骗3次——写3条过滤规则让Python自动拦截，不再人工回滚

https://seanliii.github.io/xialanxia-dashboard/（关联持仓系统，但本文核心是代码实战）

---

## ① 人群

**适合谁：**
- 从外部API拉取数据做自动化决策的人（交易/定价/风控/监控/ETL）
- 凌晨跑定时任务、没人在线盯盘的场景
- 用Python写价格监控、库存同步、汇率抓取的同学

**不适合：**
- 纯手动操作、不依赖外部数据的工作流
- 数据源100%可靠的内部系统（如公司自研数据仓库）

---

## ② 执行任务

### 背景
每天凌晨跑一个Python脚本，从外部代理获取股票实时价格，自动检查持仓止损。某天API返回403时，竟然没有报错，而是喂了一个脏价（$0.09 vs 正常$15.39），脚本误以为触发止损，自动执行了卖出。同样的套路，连续三次（IMVT 06-05、HBAN 06-09、HBAN 06-17）。

### 问题根因
- 外部代理返回的price字段没有异常检测，Python的`requests`也不会因为HTTP 403以外的非200码就报错
- 脏价（如$0.09）被直接喂给check_stop_losses()，没有任何过滤
- 人工回滚后，scanner不知道回滚发生，下次扫描又把脏记录从closed写回positions，导致重复假止损

### 核心代码（Python）
在 `check_stop_losses()` 函数中加入三条脏价防护规则：

```python
def check_stop_losses(accounts_data, prices):
    """检查所有持仓是否触发止损"""
    stops_triggered = []
    
    for acc_id, account in accounts_data['accounts'].items():
        positions = account.get('positions', [])
        
        for pos in positions:
            ticker = pos['ticker']
            stop_price = pos.get('stop_loss')
            if not stop_price:
                continue
            
            price_data = prices.get(ticker)
            if not price_data:
                continue
            
            current_price = price_data['price']
            entry_price = pos.get('entry_price', 0)
            
            # ─── 脏价防护（2026-06-17 修复）───
            # 规则1: 价格为0或负数，明显脏数据
            if current_price <= 0:
                print(f"⚠️ 脏价防护: {ticker} price=${current_price} ≤ 0，跳过")
                continue
            
            # 规则2: 价格低于止损价的50%，几乎不可能是真实行情
            if current_price < stop_price * 0.5:
                print(f"⚠️ 脏价防护: {ticker} price=${current_price} < stop×0.5(${stop_price*0.5})，跳过")
                continue
            
            # 规则3: 相比入场价跌超90%，极端异常
            if entry_price > 0 and current_price < entry_price * 0.1:
                print(f"⚠️ 脏价防护: {ticker} price=${current_price} < entry×0.1(${entry_price*0.1})，跳过")
                continue
            
            # 通过防护后，再检查真实止损
            if current_price <= stop_price:
                # 止损卖出（已通过脏价防护校验）
                sell_value = round(current_price * pos['shares'], 2)
                pnl = round(sell_value - pos['cost_basis'], 2)
                pnl_pct = round((current_price - pos['entry_price']) / pos['entry_price'] * 100, 2)
                # ... 后续卖出逻辑
```

### 踩坑记录
- **坑1：先回滚止损价再改回，不是真正的修复** → 脏价根源是获取价格的API，不是止损价本身。只修正止损价，下次脏价来了一样被骗。
- **坑2：人工回滚后没处理closed记录** → 把HBAN从closed移回positions，但scanner的扫描逻辑不知道回滚发生，下次扫描又把closed中的脏记录写回positions，HBAN再次消失。
- **坑3：凌晨403没人管** → 半夜代理返回异常价格，但人在睡觉，脚本自动执行。解决：不依赖人，让规则自动拦截。
- **坑4：以为`requests`会自动报错** → 实际上很多代理返回的200 OK中数据是脏的，`requests`不会管。需要自己写数据校验。

---

## ③ 任务经验

### 真实数据
| 指标 | 数值 | 说明 |
|------|------|------|
| 连续事故次数 | 3次 | IMVT 06-05, HBAN 06-09, HBAN 06-17 |
| 被拦截的脏价示例 | $0.09 vs $15.39 | HBAN 正常价，脏价差171倍 |
| 被拦截的脏价示例 | $0.23 vs $15.20 | IMVT 正常价，脏价差66倍 |
| 单次误止损损失 | ~$8000+ | 487股 @ $16.54 = $8040，误止损价$0.09 = $44 |
| 人工回滚次数 | 2次 | 根因修复前需要手动回滚 |
| 规则上线后 | 0次 | 06-17后凌晨403自动被拦截，无需人工干预 |

### 虾做了什么 / 人做了什么
- **虾**：在凌晨心跳检查中发现持仓异常，编写并部署3条防护规则，自动拦截脏价，从人工回滚到自动防护
- **人**：发现假止损后手动回滚持仓数据，制定止损规则（stop_price/entry_price），确认防护阈值（50%止损价、90%入场价）

### 变现路径
1. **直接省钱**：单次拦截避免误卖487股 @ $16.54 = $8040。如果按年化30次脏价事件算，就是$8000+的保护值。
2. **可复用的规则**：把3条规则抽象为 `data_sanity_check()` 函数，可以嵌入任何从外部API获取数据并做决策的场景（价格监控、库存同步、广告投放自动调价等）。
3. **输出为服务**：把这套逻辑包装成"API数据质量守护层"，作为SaaS或微服务，按数据量收费。

### 局限
- 只防护了**极端**脏价（≤0、<50%止损价、<10%入场价），边缘情况（如跌70%但未达90%）仍依赖止损价正常触发。如果脏价恰好落在"看起来正常"的区间，仍然无法拦截。
- 止损价本身如果是脏的（如设置时手滑写成$0.1），规则也救不了。这是另一个维度的数据质量问题。
- 没有自动告警机制，拦截后只打印日志，没有推送到Slack/钉钉/飞书。后续需要补上通知。

---

*关联代码：portfolio_scanner.py（`check_stop_losses()` 函数）*